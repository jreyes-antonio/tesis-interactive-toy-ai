void setup() {
  Serial.begin(115200);
  Serial.println("Iniciando ESP32 Cam Stream...");
  // Aquí configuraremos la cámara y el servidor web para el streaming
}

void loop() {
  // El código principal para mantener la conexión o procesar imágenes básicas
}
